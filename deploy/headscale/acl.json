// Default-deny multi-tenant ACL. Regenerated by `pctl mesh tenant add/rm` from the tenant registry.
// With no tenants nothing is permitted; each tenant gets a tag owner + an intra-tenant accept rule,
// so customers are isolated to their own private network and can never reach another tenant.
{
  "tagOwners": {},
  "acls": []
}
