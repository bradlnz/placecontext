# Placecontext landing site

This is the standalone public marketing site. It is intentionally separate
from the authenticated Placecontext application and links visitors to the
public installer at `https://get.placecontext.io/install.sh`.

Deploy these static files to the landing host at `placecontext.io`.
