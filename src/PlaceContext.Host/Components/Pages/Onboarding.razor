@page "/onboarding"
@attribute [Authorize(Policy = Permission.ProjectsView)]
@inject NavigationManager Nav

@code {
    protected override void OnInitialized() =>
        Nav.NavigateTo("/wiki/getting-started", replace: true);
}
