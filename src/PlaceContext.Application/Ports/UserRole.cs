namespace PlaceContext.Application.Ports;

/// <summary>
/// A member's role within an organisation.
/// </summary>
public enum UserRole
{
    Viewer = 0,
    Member = 1,
    Admin = 2,
    Owner = 3,
}
