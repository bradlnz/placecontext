<% page_title "Edit client" %>

<div class="page-header">
  <div>
    <div class="breadcrumbs">
      <%= link_to "Clients", clients_path(project_id: @project_id) %> <span>›</span>
      <%= link_to @client["name"], client_path(@client["id"], project_id: @project_id) %> <span>›</span> Edit
    </div>
    <span class="eyebrow">Customer directory</span>
    <h1>Edit client</h1>
    <p class="profile-subheading">Update contact information and lifecycle details.</p>
  </div>
</div>

<%= render "form" %>
